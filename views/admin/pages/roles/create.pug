extends ../../layouts/default.pug 
block main 
 h1 Create new role
 form(action="/admin/roles/create", method="POST")
  .form-group
    label(for="title") Title
    input(text="text", name="title", class="form-control", id="title", required)
  .form-group 
    label(for="description") Description 
    input(name="description", class="form-control", id="description", required) 
  .form-group 
    button(type="submit", class="btn btn-primary") + Add new 